<UserControl x:Class="WpfInstanceInspector.PropertyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfInstanceInspector"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="UserControl"
>
    <UserControl.Resources>
        <local:DoubleToInt32Converter x:Key="DoubleToInt32Converter" />

        <DataTemplate DataType="{x:Type system:Int32}">
            <StackPanel Orientation="Horizontal" DataContext="{Binding ElementName=UserControl}">
                <Label Content="{Binding PropertyName, Mode=OneWay }"></Label>
                <Slider Width="150"  Value="{Binding PropertyValue, Mode=TwoWay, Converter={StaticResource DoubleToInt32Converter }}" SmallChange="1"></Slider>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ListBox x:Name="lBox"></ListBox>
</UserControl>
